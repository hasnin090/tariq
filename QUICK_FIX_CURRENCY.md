# 🚨 حل سريع لمشكلة رمز العملة

## الحل الفوري (5 دقائق)

### الخطوة 1: نفّذ هذا الكود في Supabase
1. افتح [Supabase Dashboard](https://supabase.com/dashboard)
2. اختر مشروعك
3. اذهب إلى **SQL Editor**
4. انسخ والصق هذا الكود:

```sql
-- حذف رمز العملة الخاطئ نهائياً
DELETE FROM settings WHERE key = 'systemCurrency';

-- إضافة رمز صحيح
INSERT INTO settings (key, value, updated_at)
VALUES ('systemCurrency', 'IQD', NOW());
```

5. اضغط **Run** ▶️

### الخطوة 2: امسح localStorage في المتصفح
1. افتح Console (اضغط F12)
2. نفّذ هذا الكود:
```javascript
localStorage.clear();
sessionStorage.clear();
location.reload();
```

### الخطوة 3: حدّث الصفحة
اضغط Ctrl+Shift+R (أو Cmd+Shift+R في Mac)

---

## ✅ النتيجة المتوقعة
ستختفي كل تحذيرات "Invalid currency code: ریال" فوراً!

---

## 📝 ملاحظة
بعد نشر التحديثات على Netlify، لن تحتاج هذه الخطوات مرة أخرى - سيعمل التنظيف تلقائياً.
