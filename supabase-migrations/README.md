# 🗄️ Supabase Migrations - هيكل قاعدة البيانات

## 📁 الملفات الرئيسية

| الملف | الوصف | الأهمية |
|-------|-------|---------|
| `COMPLETE-DATABASE-SCHEMA.sql` | المخطط الكامل لقاعدة البيانات (24 جدول + Functions + Triggers + RLS) | ⭐⭐⭐ أساسي |
| `create-permissions-tables-and-admin.sql` | جداول الصلاحيات والمستخدم الافتراضي | ⭐⭐⭐ أساسي |
| `FINAL-storage-policies.sql` | سياسات التخزين للملفات | ⭐⭐ مهم |
| `SECURITY-FIX-remove-anon-access.sql` | إصلاحات أمنية | ⭐⭐ مهم |
| `SETUP-supabase-auth.sql` | إعداد المصادقة | ⭐⭐ مهم |
| `URGENT-remove-journal-triggers.sql` | إزالة triggers غير مطلوبة | ⭐ اختياري |

## 📂 المجلدات

### `_archive/`
ملفات SQL قديمة تم دمجها في `COMPLETE-DATABASE-SCHEMA.sql`:
- ملفات المدفوعات والأقساط
- ملفات الوحدات والمبيعات
- ملفات التحديثات الجزئية

### `_tests/`
ملفات اختبار قاعدة البيانات:
- `TEST-extra-payments-system.sql`
- `TEST-scheduled-payments-system.sql`

## 🚀 ترتيب التنفيذ

```bash
# 1. المخطط الكامل (إنشاء جميع الجداول)
psql -f COMPLETE-DATABASE-SCHEMA.sql

# 2. إعداد المصادقة
psql -f SETUP-supabase-auth.sql

# 3. الصلاحيات
psql -f create-permissions-tables-and-admin.sql

# 4. سياسات التخزين
psql -f FINAL-storage-policies.sql

# 5. الإصلاحات الأمنية
psql -f SECURITY-FIX-remove-anon-access.sql
```

## 📊 إحصائيات قاعدة البيانات

- **24 جدول** (19 أساسي + 5 إضافي)
- **90+ فهرس** لتحسين الأداء
- **6 دوال** (Functions)
- **15 مشغل** (Triggers)
- **96 سياسة أمان** (RLS Policies)

## 🔐 المستخدم الافتراضي

```
username: admin
password: 123456
role: Admin
```

⚠️ **تنبيه**: غيّر كلمة المرور فوراً بعد التشغيل!
