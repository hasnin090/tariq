# 🚀 البدء السريع - نظام الدفع الإضافي والمرفقات

## ⚡ خطوات التشغيل السريعة

### 1️⃣ تطبيق Migration
```bash
# في Supabase SQL Editor، نفذ محتوى هذا الملف:
supabase-migrations/add-payment-attachments-and-extra-payments.sql
```

أو باستخدام CLI:
```bash
supabase db push
```

### 2️⃣ التحقق من التثبيت
افتح Supabase SQL Editor ونفذ:
```sql
-- التحقق من الجداول الجديدة
SELECT table_name 
FROM information_schema.tables 
WHERE table_name IN ('payment_attachments', 'extra_payments');

-- التحقق من الدوال
SELECT routine_name 
FROM information_schema.routines 
WHERE routine_name LIKE '%payment%';
```

### 3️⃣ اختبار النظام

#### ✅ اختبار رفع المرفقات:
1. انتقل إلى: **الدفعات المجدولة**
2. اضغط على **"تسديد"** لأي قسط
3. جرب رفع صورة أو PDF
4. يمكنك الضغط **"تخطي"** للتسديد بدون مرفق

#### ✅ اختبار الدفع الإضافي:
1. في **الدفعات المجدولة**
2. اختر حجز من القائمة
3. اضغط زر **"دفع إضافي"** (سيظهر بعد اختيار الحجز)
4. أدخل مبلغ وجرب كلا الخيارين:
   - **تلقائي**: سيوزع المبلغ تلقائياً
   - **يدوي**: اختر عدد الأقساط والفترة

---

## 📋 نظرة سريعة على الميزات

| الميزة | الوصف | الحالة |
|--------|-------|--------|
| 📎 رفع المرفقات | إرفاق إيصالات/فواتير مع كل دفعة | ✅ اختياري |
| 💰 دفع إضافي | دفع مبلغ خارج الجدول | ✅ مع إعادة جدولة |
| 🔄 إعادة جدولة تلقائية | توزيع المتبقي على الأقساط الحالية | ✅ |
| ⚙️ إعادة جدولة يدوية | إنشاء جدول جديد كامل | ✅ |
| 🎯 تسديد كامل تلقائي | عند دفع مبلغ ≥ المتبقي | ✅ |

---

## 🎬 أمثلة سريعة

### مثال 1: تسديد عادي
```
الضغط على "تسديد" → رفع مرفق (أو تخطي) → تم! ✅
```

### مثال 2: دفع إضافي
```
اختيار حجز → "دفع إضافي" → إدخال مبلغ → اختيار نوع الجدولة → تأكيد ✅
```

---

## ⚠️ نقاط مهمة

- ✅ **المرفقات اختيارية** - لن توقف عملية الدفع
- ✅ **حد أقصى للملف: 10MB**
- ✅ **أنواع مدعومة**: صور، PDF، Word
- ✅ **إعادة الجدولة اليدوية تحذف الأقساط المعلقة القديمة**
- ✅ **الدفع الكامل يسدد جميع الأقساط تلقائياً**

---

## 📚 للمزيد من التفاصيل

راجع الدليل الشامل:
```
docs/EXTRA_PAYMENTS_AND_ATTACHMENTS_GUIDE.md
```

---

## 🐛 مشاكل شائعة

**لا يظهر زر "دفع إضافي"؟**
→ تأكد من اختيار حجز من القائمة

**فشل رفع الملف؟**
→ تحقق من الحجم (< 10MB) ونوع الملف

**خطأ في إعادة الجدولة؟**
→ راجع console للتفاصيل

---

✨ **جاهز للاستخدام!**
