# ⚡ دليل سريع: نقل المستندات إلى مشروع الفندق

## 🎯 الهدف
نقل جميع المستندات الموجودة حالياً (التي لا تحتوي على project_id) إلى مشروع الفندق.

---

## 📋 الخطوات السريعة

### 1️⃣ افتح SQL Editor في Supabase
- اذهب إلى لوحة تحكم Supabase
- افتح `SQL Editor`

### 2️⃣ نفذ Migration Script
- انسخ محتوى الملف: `supabase-migrations/migrate-documents-to-hotel-project.sql`
- الصق المحتوى في SQL Editor
- اضغط `Run`

### 3️⃣ تحقق من النتائج
بعد تنفيذ السكريبت، ستظهر لك رسالة مثل:
```
NOTICE: تم نقل X مستند(ات) إلى مشروع الفندق (ID: ...)
```

### 4️⃣ تأكد من النتائج (اختياري)
نفذ هذا الاستعلام للتحقق:
```sql
SELECT 
    p.name as project_name,
    COUNT(d.id) as document_count
FROM projects p
LEFT JOIN documents d ON p.id = d.project_id
GROUP BY p.id, p.name
ORDER BY document_count DESC;
```

---

## ✅ ماذا بعد؟

بعد تنفيذ الـ Migration:

1. **المستندات القديمة:** ✅ تم نقلها لمشروع الفندق
2. **المستندات الجديدة:** ✅ ستُربط تلقائياً بالمشروع النشط
3. **التصفية:** ✅ المستندات تُعرض مُفلترة حسب المشروع المختار

---

## 🔍 كيفية التحقق في الواجهة

1. افتح صفحة **الحسابات / المستندات**
2. اختر **مشروع الفندق** من القائمة المنسدلة في الأعلى
3. يجب أن تظهر جميع المستندات القديمة

---

## ⚠️ ملاحظات مهمة

- ✅ السكريبت آمن ولن يؤثر على المستندات التي لديها `project_id` بالفعل
- ✅ يتم البحث تلقائياً عن مشروع يحتوي "فندق" أو "hotel" في اسمه
- ✅ إذا لم يُعثر على المشروع، سيظهر خطأ واضح

---

## 🛠️ في حالة المشاكل

### المشكلة: "لم يتم العثور على مشروع الفندق"
**الحل:**
1. تحقق من وجود مشروع باسم يحتوي "فندق"
2. أو استخدم الطريقة اليدوية:
```sql
-- عرض جميع المشاريع
SELECT id, name FROM projects;

-- نقل المستندات يدوياً (استبدل ID_HERE بالـ ID الصحيح)
UPDATE documents 
SET project_id = 'ID_HERE' 
WHERE project_id IS NULL;
```

---

## 📚 للمزيد من المعلومات

راجع الدليل الكامل في:
📄 `docs/DOCUMENTS_PROJECT_ORGANIZATION_GUIDE.md`

---

**وقت التنفيذ المتوقع:** أقل من دقيقة  
**مستوى الصعوبة:** ⭐ سهل  
**الأمان:** ✅ آمن (لا يحذف أي بيانات)
